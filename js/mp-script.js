document.getElementById("db-btn").addEventListener("click", function() {
    // Redirect to another page when the search button is clicked
    window.location.href = "dashboard.html"; // Redirects to a new page, you can change the URL here
});